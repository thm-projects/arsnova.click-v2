/*import { TestBed } from '@angular/core/testing';

import { LocalStorageReceiveService } from './local-storage-receive.service';

describe('LocalStorageReceiveService', () => {
  let service: LocalStorageReceiveService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(LocalStorageReceiveService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});*/
