import { UnusedKeyFilterPipe } from './unused-key-filter.pipe';

describe('UnusedKeyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnusedKeyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
