import { QuizFilterPipe } from './quiz-filter.pipe';

describe('QuizFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
